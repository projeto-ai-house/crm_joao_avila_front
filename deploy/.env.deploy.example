# ========================================
# CONFIGURAÇÕES DO SERVIDOR VPS
# ========================================

# Endereço IP ou domínio da VPS
VPS_HOST=seu-servidor.com

# Porta SSH (padrão: 22)
VPS_PORT=22

# Usuário SSH
VPS_USERNAME=root

# OPÇÃO 1: Autenticação por senha
VPS_PASSWORD=sua_senha_aqui

# OPÇÃO 2: Autenticação por chave privada (mais seguro)
# Descomente e configure se usar chave SSH
# VPS_PRIVATE_KEY_PATH=/caminho/para/sua/chave_privada

# ========================================
# CONFIGURAÇÕES DO PROJETO
# ========================================

# Diretório remoto onde o projeto será implantado
VPS_REMOTE_DIR=/var/www/crm-joao-avila

# Nome do arquivo docker-compose (padrão: docker-compose.yml)
DOCKER_COMPOSE_FILE=docker-compose.yml

# Nome do projeto (usado para logs)
PROJECT_NAME=CRM João Avila

# ========================================
# DICAS DE SEGURANÇA
# ========================================
# 1. NÃO commite este arquivo com credenciais reais
# 2. Use chave SSH ao invés de senha quando possível
# 3. Considere usar variáveis de ambiente do sistema
# 4. Mantenha permissões restritas neste arquivo (chmod 600)
